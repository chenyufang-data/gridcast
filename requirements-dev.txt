# Everything for local development: backend + frontend + tests + tooling
-r requirements.txt
-r requirements-frontend.txt
matplotlib>=3.8
pytest>=8.0
httpx>=0.27             # fastapi TestClient transport
ruff>=0.6
mypy>=1.10
uv>=0.4                 # lock-file generation (uv pip compile --universal)
xgboost>=2.1            # models/tabular.py "xgb" estimator (experiments; LightGBM stays the default)
