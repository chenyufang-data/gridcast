# Forced light theme: chat bubbles and the plot palette are designed for a
# light background; without this, browsers in dark mode get night mode.
[theme]
base = "light"
primaryColor = "#2a78d6"

# Public demo: no "Deploy" button or developer menu for visitors
[client]
toolbarMode = "minimal"
