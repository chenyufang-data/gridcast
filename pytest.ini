[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not live" -q
markers =
    live: touches the network (NYISO archive, Open-Meteo, LLM APIs); run explicitly with `pytest -m live`
    slow: real LightGBM training or multi-day backtests; deselect with `-m "not slow"`
